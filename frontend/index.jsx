import React from 'react';
import ReactDOM from 'react-dom/client';
import AuroraHUD from './AuroraHUD';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <AuroraHUD />
  </React.StrictMode>
);
